#!/bin/sh

self_addr=$(ifconfig | grep 'inet addr:' | head -1 | xargs -n1 | sed -n 2p | tr -d 'addr:')

echo "self addr: $self_addr"

export RELEASE_DISTRIBUTION=name
export RELEASE_NODE=smache@${self_addr}
export RELEASE_COOKIE=$COOKIE
