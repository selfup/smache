# Common notes for minikube

```bash
minikube start
minikube stop
minikube ssh
```

As a workaround to building locally and not pushing to dockerhub

Send local docker image to minikube

Consider GitLab registry??
