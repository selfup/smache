version: '3.1'

services:
  prod:
    build:
      context: .
      dockerfile: Dock.smache.Dockerfile
    command: bash -c "ls -lah && elixir rel/vm_args.exs && _build/prod/rel/smache/bin/smache foreground"
    ports:
      - "1234:4000"
    volumes:
    - /tmp:/tmp
  